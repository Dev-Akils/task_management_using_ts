# core
npm install react react-dom react-router-dom

# types + ts
npm install -D typescript @types/react @types/react-dom @types/react-router-dom

# redux toolkit + react-redux
npm install @reduxjs/toolkit react-redux

# drag & drop (modern, flexible)
npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/modifiers @dnd-kit/accessibility

# date utilities
npm install date-fns

# UI (optional)
npm install @mui/material @emotion/react @emotion/styled
# OR use Tailwind CSS if you're preferring that (you've used it earlier)
